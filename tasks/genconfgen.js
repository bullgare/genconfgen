"use strict";
module.exports = function (grunt) {
	var ConfGenerator = require('../lib/generator.js');

	grunt.registerMultiTask('genconfgen', 'Generates custom configs with provided config generators', function () {

	});
};